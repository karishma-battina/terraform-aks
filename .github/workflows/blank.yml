# .github/workflows/manual-job.yml
name: Manual Job Example

on:
  workflow_dispatch:  # Allows manual triggering via GitHub UI/API

jobs:
  manual_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "This job runs manually! 🚀"
